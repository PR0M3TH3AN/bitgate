{
  "name": "@bitgate/site",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "BitGate landing page, integration guide, and live demo — a plain static site consuming the built widget.",
  "scripts": {
    "build": "node build.mjs",
    "dev": "node build.mjs && node serve.mjs"
  }
}
